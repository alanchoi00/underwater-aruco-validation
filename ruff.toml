line-length = 90

[lint]
# E701/E702 object to `a = 1; b = 2`, which this repo uses deliberately in places.
ignore = ["E701", "E702"]

[lint.per-file-ignores]
# The collage is written in a dense one-line style throughout; reformatting it would
# churn every line of a working, verified file.
"target/aruco_collage_a4.py" = ["E401", "E741"]
